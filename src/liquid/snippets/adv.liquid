<style> 
    .icon_left-- { 
        background-image: url({{ settings.icon_left | img_url: "master" }})
    }
    .icon_right-- { 
        background-image: url({{ settings.icon_right | img_url: "master" }})
    }
    </style>


<section class="advert"> 
    <div class="contain">
        <h1 class="text_adv">{{ settings.adv_title }}</h1>
        <div class="boxes"> 
            <div class="box_left">
                <div>
                <h3>{{ settings.left_title}}</h3> 
                <p> {{ settings.left_text }}</p>
                </div>
                <div class="icon_left icon_left--"></div>
            </div>
            <div class="box_right">
                <div>
                <h3>{{ settings.right_title}}</h3> 
                <p> {{ settings.right_text}}</p>
                </div>
                <div class="icon_right icon_right--"></div>
            </div>
        </div>
    </div>
</section>

   